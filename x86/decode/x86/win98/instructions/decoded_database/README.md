# Decoded instruction database — Windows 98 boot

Generated by `x86/tools/decode_db.py` from the deduplicated encoding
corpus that an instrumented v86 collects during a live boot
(`x86/decoders/v86/src/rust/l8trace.rs`, feature `l8trace`).

Each row is a **distinct instruction byte string** that Windows 98
actually executed, decoded into its constituent fields with their
bit offsets, so another decoder can be diffed against it field by
field rather than by mnemonic.

## Files

| file | content |
|---|---|
| `win98.corpus.jsonl` | raw corpus from the emulator: encoding shape -> up to 2 sample byte strings + execution count |
| `win98.decoded.jsonl` | one JSON record per distinct byte string, all fields |
| `win98.decoded.tsv` | the same, flat; the `fields` column is `name@bitoffset:width=value` |
| `win98.l8_expect.csv` | the l8 test vector: bytes in -> expected `decode(d)` arm, bound arguments and `end` (produced by `x86/tools/l8_expect.py`) |
| `win98.l8_gaps.md` | which encodings the l8 arm list does not yet cover, and the `end` verdict |

The `.csv` is column-aligned with spaces so it reads as a table; it
is still valid CSV (`csv.reader(f, skipinitialspace=True)`), the
values just need `.strip()`.

## Size

15626 distinct byte strings covering 487,558,689 executed instructions.

`count` belongs to the **encoding shape** (`shape` column), not to
the individual byte string: the emulator counts shapes and keeps up
to two byte strings per shape as displacement/immediate samples, so
two rows sharing a `shape` each report that shape's total and the
figure above double-counts them.  Use `count` as a ranking within a
shape-deduplicated view; for exact dynamic frequencies see the
caveat at the end.

## Validation

Every record is decoded twice — a positional/structural decode
written from the bytes alone, and iced-x86 — and the two are
required to agree on base/index/scale, displacement value and
width, and total length.

| | |
|---|---|
| samples | 19573 |
| ok | 15626 |
| mismatch | 0 |
| page_truncated | 46 |
| undecodable | 0 |

`page_truncated` are samples the emulator cut short at a page
boundary (it will not translate a second page just to trace), not
instructions NT failed to execute.

## Coverage of the addressing-mode partition

This is the axis a bit-level decoder's modrm arms partition on, so
it is the axis on which the database has to be complete.

| modrm kind | distinct encodings | executed |
|---|---:|---:|
| reg_direct | 3284 | 100,663,066 |
| base_d8 | 2848 | 51,372,037 |
| base16_d8 | 1767 | 8,418,587 |
| sib_d8 | 1085 | 8,044,424 |
| base | 960 | 15,863,081 |
| sib_base | 950 | 1,402,910 |
| base_d32 | 942 | 1,301,896 |
| abs16 | 880 | 5,894,213 |
| base16 | 592 | 4,249,827 |
| base16_d16 | 534 | 523,085 |
| abs32 | 465 | 13,082,960 |
| sib_abs | 302 | 762,799 |
| sib_d32 | 244 | 246,055 |

## Modes

| mode | distinct encodings |
|---|---:|
| 16-bit | 6695 |
| 32-bit | 8931 |

## First appearance, by boot phase (60 s each)

An encoding counted in phase *n* was executed for the first time in
that phase, so this shows the ISA surface growing: firmware and the
real-mode loader first, then the kernel, then the GUI.

| phase | new encodings |
|---:|---:|
| 1 | 13296 |
| 2 | 2311 |
| 3 | 18 |
| 4 | 1 |

## Distinct opcodes

281.

## Hottest 30 shapes

| count | mode | bytes | text |
|---:|---|---|---|
| 23,420,024 | 32 | `c3` | `ret` |
| 16,070,001 | 32 | `ebf6` | `jmp short 0xFFFFFFF8` |
| 12,246,798 | 32 | `33c0` | `xor eax, eax` |
| 8,854,289 | 32 | `e8f25effff` | `call 0xFFFF5EF7` |
| 8,682,478 | 32 | `56` | `push esi` |
| 8,082,246 | 32 | `5e` | `pop esi` |
| 7,524,376 | 32 | `fa` | `cli` |
| 6,958,484 | 32 | `9c` | `pushf` |
| 6,914,158 | 32 | `9d` | `popf` |
| 6,209,668 | 32 | `0306` | `add eax, [esi]` |
| 6,180,924 | 32 | `f7d8` | `neg eax` |
| 6,167,420 | 32 | `33d2` | `xor edx, edx` |
| 6,136,492 | 32 | `86e0` | `xchg ah, al` |
| 6,092,298 | 32 | `8ae0` | `mov ah, al` |
| 6,089,132 | 32 | `e421` | `in al, 0x21` |
| 5,108,698 | 32 | `7406` | `je short 8` |
| 4,364,399 | 32 | `7539` | `jne short 0x0000003B` |
| 4,351,659 | 32 | `771b` | `ja short 0x0000001D` |
| 3,757,780 | 16 | `a801` | `test al, 1` |
| 3,459,438 | 16 | `745b` | `je short 0x005D` |
| 3,245,682 | 32 | `e621` | `out 0x21, al` |
| 3,215,358 | 32 | `be10270000` | `mov esi, 0x2710` |
| 3,205,911 | 32 | `2b4638` | `sub eax, [esi+0x38]` |
| 3,201,794 | 32 | `8946fc` | `mov [esi-4], eax` |
| 3,072,748 | 32 | `3b4640` | `cmp eax, [esi+0x40]` |
| 2,941,208 | 32 | `135604` | `adc edx, [esi+4]` |
| 2,180,532 | 32 | `50` | `push eax` |
| 2,124,219 | 16 | `7509` | `jne short 0x000B` |
| 1,891,639 | 32 | `7203` | `jb short 5` |
| 1,634,222 | 32 | `57` | `push edi` |

## Caveat: the JIT path

The corpus is collected on v86's **interpreter** path. v86 only
compiles a block after it has been interpreted enough times to get
hot, so every executed encoding passes through the interpreter at
least once and the *set* of encodings is complete; the `count`
column, however, undercounts hot code by the compiled fraction.
Counts are therefore a ranking, not a census — for exact dynamic
frequencies use `win98.hist.tsv`, which also instruments the JIT.
